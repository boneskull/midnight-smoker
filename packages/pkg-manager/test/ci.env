# TODO: replace this with a script that conditionally sets the reporter
# based on the presence of both the CI and GITHUB env vars.

DEBUG=""
NODE_OPTIONS="--test-reporter node-test-github-reporter"
FORCE_COLOR="1"
